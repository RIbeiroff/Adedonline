<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jadso.adedonline">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SalaCreateActivity"
            android:label="@string/title_activity_sala_create"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InserirTemaActivity" />
        <activity
            android:name=".SalaEntrar"
            android:label="Entrar numa sala" />
        <activity android:name=".AlterarTemaActivity" />
        <activity android:name=".SalaIniciarActivity" />
        <activity android:name=".SalaLetraSorteada" />
        <activity android:name=".SalaLetraSorteadaServidorActivity" />
        <activity android:name=".ResponderActivity"></activity>
    </application>

</manifest>